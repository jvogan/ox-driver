---
description: Run the minimum read-only Ox Driver team acceptance check
argument-hint: "<small project question>"
---

Run a read-only Ox Driver smoke test for: $ARGUMENTS

Stop unless the fixture is disposable and non-sensitive and the user has
acknowledged that the governing Stealth EULA permits collection, retention,
sharing, training, evaluation, and improvement of submitted content.

Expected range: five to nine model calls. Stop and disclose before launch if the
estimate exceeds ten. Confirm `OX_DRIVER_GUARD_READY=1` and report the exact
`PI_PROVIDER`, `PI_MODEL`, and `PI_REASONING_LEVEL` environment values.

Make one direct `subagent` call with `agent: "pi-lead"`,
`agentScope: "user"`, and `async: false`. Do not use `workflowScript`. Require
the lead to launch exactly two direct `pi-agent` leaves: one maps
the relevant implementation and one reviews its tests or risks. Every child task
must say: "Read-only: do not edit, modify, write, or touch files." Require
file-and-line evidence. Integrate the reports, rerun a read-only working-tree
check, and state whether the tree stayed clean.
